import { createTheme } from '@rneui/themed';

const theme = createTheme({
  lightColors: {
    primary: '#2F6CE0',
    secondary: '#2F6CE0',
  },
});

export default theme;

const Colors = {
  line: '#eee', // 分割线
  primary: '#2F6CE0', 
  backgroundColor: '#F4F4F4',
  title: '#333',
};

const Sizes = {
  tabs: { width: 24, height: 24 },
};

export { Colors, Sizes };
